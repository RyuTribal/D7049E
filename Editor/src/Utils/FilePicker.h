#pragma once
#include <string>
#include <vector>


namespace Editor {
	class FilePicker
	{
	public:
		static std::string OpenFileExplorer(std::vector<std::vector<std::string>>& filter);
	private:

	};
}
